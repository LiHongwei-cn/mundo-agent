#!/bin/bash
# MUNDO Agent v2.2.1 — macOS .app 启动脚本

SCRIPT_PATH="$HOME/.hermes/mundo-agent/mundo.sh"

osascript <<EOF
tell application "Terminal"
    activate
    do script "bash \"$SCRIPT_PATH\""
    set custom title of front window to "MUNDO — THE EMPEROR"
end tell
EOF
